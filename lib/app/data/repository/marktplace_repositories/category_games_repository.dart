import 'package:cGamer/app/data/provider/marketplace_providers/category_games_provider.dart';
import 'package:meta/meta.dart';

class CategoryGamesRepository {
  final CategoryGamesProvider apiClient;

  CategoryGamesRepository({required this.apiClient});

  // getAll() {
  //   return apiClient.getAll();
  // }

  // getId(id) {
  //   return apiClient.getId(id);
  // }

  // delete(id) {
  //   return apiClient.delete(id);
  // }

  // edit(obj) {
  //   return apiClient.edit(obj);
  // }

  // add(obj) {
  //   return apiClient.add(obj);
  // }
}
